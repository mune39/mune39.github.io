# 桜木町 分配シミュレーター (GitHub Pages)

スマホ向けの月次分配シミュレーター。単一の `index.html` だけで動作し、
外部依存なし・ログイン不要。GitHub Pages でそのまま公開できます。

- 公開URL: https://mune39.github.io/
- 保存機能（PDF / CSV）は通常のブラウザのダウンロードで動作。
- 更新は `index.html` を差し替えて `git push` するだけ。
